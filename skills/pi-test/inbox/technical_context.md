# Technical Context

The junior developer is expected to build the backend API first.

Preferred technology:

- Python
- FastAPI
- PostgreSQL
- Docker or Podman
- Basic REST API

Authentication can be simple for the first prototype.

The application may eventually be deployed on OpenShift, but the first version can run locally.
